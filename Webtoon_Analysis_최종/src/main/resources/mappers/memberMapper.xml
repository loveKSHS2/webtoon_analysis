<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.webtoon.mapper.memberMapper">
	<select id="selectMember" resultType="memberVO">
		SELECT title,writer,episode,genre,upload, age_limit, img, starScore, total FROM info
	</select>
    
</mapper>